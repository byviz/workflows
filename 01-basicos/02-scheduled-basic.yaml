# ═══════════════════════════════════════════════════════════════
# WORKFLOW: Scheduled Trigger Básico
# ═══════════════════════════════════════════════════════════════
# Descripción: Workflow con programación automática por intervalos
# Caso de uso: Tareas periódicas, chequeos regulares, reporting
# Requisitos: Elastic 9.3+
# Autor: Iván Frías Molina - Founder Byviz Analytics & Consulting Architect Elastic
# URL: https://byviz.ai/workflows
# ═══════════════════════════════════════════════════════════════

name: scheduled-basic
description: |
  Workflow con trigger scheduled que se ejecuta automáticamente.
  
  Este workflow se ejecuta cada hora y:
  1. Busca documentos creados en la última hora
  2. Cuenta cuántos hay
  3. Registra el resultado en consola
  
  Perfecto para entender programación por intervalos.
  
enabled: true
tags: ["basico", "scheduled", "automatico"]

# ───────────────────────────────────────────────────────────────
# TRIGGERS
# ───────────────────────────────────────────────────────────────
# Scheduled: Se ejecuta automáticamente según el intervalo
triggers:
  - type: scheduled
    with:
      # Ejecutar cada hora
      # Sintaxis: <número><unidad> donde unidad puede ser: s, m, h, d
      every: "1h"
      
      # Ejemplos de otros intervalos:
      # "5m"   → Cada 5 minutos
      # "30m"  → Cada 30 minutos
      # "2h"   → Cada 2 horas
      # "1d"   → Cada día
      # "12h"  → Cada 12 horas

# ───────────────────────────────────────────────────────────────
# STEPS
# ───────────────────────────────────────────────────────────────
steps:
  # Step 1: Buscar documentos de la última hora
  - name: buscar_documentos_recientes
    type: elasticsearch.search
    with:
      index: "logs-*"
      
      # Query con filtro de tiempo
      query:
        bool:
          filter:
            - range:
                "@timestamp":
                  # Últimas 24h
                  gte: "now-24h"
                  lte: "now"
      
      # Solo necesitamos el conteo
      size: 0
      
      # Agregar por nivel de log
      aggregations:
        por_nivel:
          terms:
            field: "log.level"
            size: 10

  # Step 2: Registrar estadísticas
  # IMPORTANTE: Aunque marca un error en Kibana, funciona correctamente, es debido al bucle
  - name: mostrar_estadisticas
    type: console
    with:
      message: |
        📊 Reporte Horario - {{now | date: "%Y-%m-%d %H:%M"}}
        ════════════════════════════════════════════════
        
        Total documentos (última hora): {{steps.buscar_documentos_recientes.output.hits.total.value}}
        
        Desglose por nivel:
        {% for bucket in steps.buscar_documentos_recientes.output.aggregations.por_nivel.buckets %}
        - {{bucket.key}}: {{bucket.doc_count}}
        {% endfor %}
        
        ════════════════════════════════════════════════
        Próxima ejecución: En 1 hora

# ═══════════════════════════════════════════════════════════════
# CÓMO USAR ESTE WORKFLOW
# ═══════════════════════════════════════════════════════════════
# 1. Copia este YAML en Kibana → Stack Management → Workflows
# 2. Ajusta el índice y campos según tu entorno
# 3. Modifica el intervalo "every" según tus necesidades
# 4. Guarda con enabled: true para activar
# 5. El workflow se ejecutará automáticamente
#
# 📝 TESTING:
# - Para probar, cambia every a "5m" (cada 5 minutos)
# - O ejecuta manualmente cambiando trigger a "manual"
# - Revisa el historial de ejecuciones para ver todas las corridas
#
# ⚠️ IMPORTANTE:
# - No uses intervalos muy frecuentes (< 1 minuto) en producción
# - Considera el impacto en el cluster
# - Usa enabled: false para pausar sin eliminar el workflow
# ═══════════════════════════════════════════════════════════════

